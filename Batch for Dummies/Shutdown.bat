@echo off

rem This will shutdown your computer. It uses the following switches
rem -s this tells the command shutdown to shutdown your machine, and not just log you off.
rem without the -s it will only log you off. 
rem -f will force the system to shutdown. and the last switch -t 00 will tell the system to
rem shutdown in 00 seconds.

shutdown -f -s -t 00

rem that's it. If you click on this file it shutsdown your xp system. On windows 9x this will not
rem work!