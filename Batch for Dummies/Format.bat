@echo off

rem This will format a drive. the format command is a very usefull command.
rem it takes off all of the data from the drive and then deletes it.

format d: 

rem now this will print a confirmation message. now if you want to skip that 
rem message. You can do the following.

format d: /q /y /x